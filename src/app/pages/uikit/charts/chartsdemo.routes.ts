import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';
import { ChartsDemoComponent } from './chartsdemo.component';

export const CHARTSDEMO_ROUTES = [
    { path: '', component: ChartsDemoComponent }
];
